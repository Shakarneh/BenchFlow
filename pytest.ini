# Tells pytest-django which settings to load. Only tests marked with
# @pytest.mark.django_db touch the database -- the 621 domain tests
# keep running with no Django at all.
[pytest]
DJANGO_SETTINGS_MODULE = config.settings
